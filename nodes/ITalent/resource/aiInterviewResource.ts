import { ResourceOptions } from '../../help/type/IResource';

const AiInterviewResource: ResourceOptions = {
	name: 'AI面试官',
	value: 'aiInterview',
	order: 200,
};

export default AiInterviewResource;

